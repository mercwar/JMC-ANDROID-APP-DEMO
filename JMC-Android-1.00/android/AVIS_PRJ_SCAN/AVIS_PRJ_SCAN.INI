; ============================================================================
; AVIS PROJECT SCAN CONFIGURATION — AVIS COORD EDITION - MERCWAR01@GMAIL.COM
; ============================================================================
; ARCH: x86_64 | GROUP ID: FFF | OWNER: Joseph Michael Catalano
; ============================================================================
;  This configuration file controls how the AVIS Project Scanner interprets,
;  resolves, and validates directory paths, root mappings, and user‑mode
;  selection keys. Every field in this file is authoritative. If the scanner
;  behaves unexpectedly — HIDING, SKIPPING, or THROWING AWAY a directory —
;  the cause is almost always inside this INI.
; ============================================================================


[AVIS_COORD]
; Reserved for future AVIS COORDINATOR metadata.
; This block is intentionally left open for expansion.
; Do NOT remove this header.


[DIR]
; ================================================================
;  THROWS ’n HIDES — IMPORTANT NOTES FOR THE [DIR] SECTION
; ================================================================
;  The AVIS scanner is strict. If a directory is:
;      * SKIPPED
;      * HIDDEN
;      * THROWN AWAY
;  then the FIRST place to check is this [DIR] section.
;
;  This INI does NOT create directories — it only DEFINES them.
;  If a directory does not physically exist on disk, the scanner
;  may ignore it, hide it, or treat it as invalid depending on
;  the scan mode and UMSK rules.
;
;  If you see .BK or .TMP versions of this INI, READ THEM.
;  They may contain the last known good configuration before a
;  failed write, interrupted scan, or partial update.
;
;  Examples of entries that may be skipped or hidden:
;      DIR_TEST=/TEST
;      H:\htdocs\robo-knight\Sentinel\AVIS_PRJ_SCAN\LOG
;      DIR_TEST=D:\msys64\home\Demonizer\cvbgod.gemini\TEST
;DIR_TEST=C:\TEST     <-- UNCOMMENT AND FIX THIS TO TEST
;
;  CRITICAL RULE:
;      DO NOT USE LEADING SPACES IN ANY KEY.
;      If a key begins with a space, the record WILL FAIL and the
;      scanner will silently ignore the entry.
;
;  inValid example:
;      DIR_TEST=<ROOT>
;      <ROOT> is determined automatically by the scanner. You do NOT
;      manually supply ROOT — the scanner resolves it from the drive
;      where AVIS_PRJ_SCAN lives.
;
;  Valid example: [FROM auto‑DETECTED DRIVE where the scanner lives]
;  uncomment the line below to test
;DIR_TEST=D:\test
;
;  NOTE:
;      The [DIR] section MUST contain absolute paths with drive letters.
;      Relative paths, symbolic placeholders, or missing drive letters
;      will be ignored or treated as hidden.
; ================================================================


;[UMSK]
;PRJ_APP_KEY=AVIS_PRJ_SCAN <---- do not modify
;PRJ_DRV_KEY=MyProjects     <---- PRJ_DIR_KEY lives in this directory
;PRJ_DIR_KEY=MyVBProjects   <---- PRJ_ROOT_KEY lives in this directory
;PRJ_ROOT_KEY=\MyVBProject\MyVBProjectSub1\MyVBProjectsub2
;    <----- PRJ_APP_KEY must live in this final directory.
;
;  Your physical layout should be:
;      <drive>:\MyProjects\MyVBProjects\MyVBProject\MyVBProjectSub1\MyVBProjectsub2
;
;  AVIS_PRJ_SCAN.bat and /AVIS_PRJ_SCAN must live in the physical path.
;  The [DIR] section of the INI must always include the drive letter.
;  The scanner resolves ROOT based on the physical location of the app.
; ================================================================
[DIR]


[CVBGOD]
; Signature and Identity markers
; This block is reserved for ceremonial identity, lineage, and
; CVBGOD‑specific metadata. Do NOT remove this header.

[UMSK]
PRJ_DRV_KEY=Apache24
PRJ_DIR_KEY=htdocs
PRJ_ROOT_KEY=\android
PRJ_APP_KEY=AVIS_PRJ_SCAN

